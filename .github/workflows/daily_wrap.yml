name: daily_wrap

# 19:00 IST Mon-Fri = 16:00 UTC Mon-Fri (§B.4 locked).
on:
  schedule:
    - cron: '0 16 * * 1-5'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_run_report.yml
    with:
      report_type: daily_wrap
    secrets: inherit
