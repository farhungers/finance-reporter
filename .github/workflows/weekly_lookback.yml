name: weekly_lookback

# 16:00 IST Saturday = 13:00 UTC Saturday (§B.4 locked).
on:
  schedule:
    - cron: '0 13 * * 6'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_run_report.yml
    with:
      report_type: weekly_lookback
    secrets: inherit
